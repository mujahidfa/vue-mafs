import { defineConfig } from "vitepress";

export default defineConfig({
  lang: "en-US",
  title: "Vue Mafs",
  description: "Vue components for interactive math.",
  lastUpdated: true,
  themeConfig: {
    siteTitle: "Vue Mafs",
  },
});
